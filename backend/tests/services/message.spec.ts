import "mocha"
import { assert } from "chai"

describe("MessageService tests", () => {
  /**
   * Create message tests
   */

  it("Should create message", () => {
    assert.fail("Not implemented")
  })

  it("Should fail to create message", () => {
    assert.fail("Find by id not implemented")
  })

  /**
   * Find message tests
   */

  it("Should find message", () => {
    assert.fail("Find by id not implemented")
    assert.fail("Find by sender id not implemented")
    assert.fail("Find by discussion id not implemented")
  })

  it("Should fail to find unexisted message", () => {
    assert.fail("Find by id not implemented")
    assert.fail("Find by sender id not implemented")
    assert.fail("Find by discussion id not implemented")
  })

  /**
   * Update message tests
   */

  it("Should update message", () => {
    assert.fail("Update by id not implemented")
  })

  it("Should fail to update unexisted message", () => {
    assert.fail("Update by id not implemented")
  })

  /**
   * Remove message tests
   */

  it("Should remove message", () => {
    assert.fail("Remove by id not implemented")
    assert.fail("Remove by sender id not implemented")
    assert.fail("Remove by discussion id not implemented")
  })

  it("Should fail to remove unexisted message", () => {
    assert.fail("Remove by id not implemented")
    assert.fail("Remove by sender id not implemented")
    assert.fail("Remove by discussion id not implemented")
  })
})