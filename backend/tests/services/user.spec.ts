import "mocha"
import { assert } from "chai"

describe("User tests", () => {

  /**
   * Find user tests
   */

  it("Should find user", () => {
    assert.fail("Find by id not implemented")
    assert.fail("Find by username not implemented")
  })

  it("Should fail to find unexisted user", () => {
    assert.fail("Find by id not implemented")
    assert.fail("Find by username not implemented")
  })

  /**
   * Update user tests
   */

  it("Should update user", () => {
    assert.fail("Update by id not implemented")
    assert.fail("Update by username not implemented")
  })

  it("Should fail to update unexisted user", () => {
    assert.fail("Update by id not implemented")
    assert.fail("Update by username not implemented")
  })

  /**
   * Remove user tests
   */

  it("Should remove user", () => {
    assert.fail("Remove by id Not implemented")
    assert.fail("Remove by username not implemented")
  })

  it("Should fail to remove unexisted user", () => {
    assert.fail("Remove by id not implemented")
    assert.fail("Remove by username not implemented")
  })
})
